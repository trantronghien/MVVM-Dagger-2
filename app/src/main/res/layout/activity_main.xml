<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">
    <data>

        <import type="android.view.View" />
        <import type="com.example.hientt17.example.model.GitHubRepository.Owner"/>

        <variable
            name="modelGithub"
            type="com.example.hientt17.example.model.GitHubRepository" />
    </data>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="center_horizontal">

        <TextView
            android:id="@+id/textView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentLeft="true"
            android:layout_alignParentStart="true"
            android:layout_alignParentTop="true"
            android:layout_marginLeft="34dp"
            android:layout_marginStart="34dp"
            android:layout_marginTop="20dp"
            android:visibility="@{modelGithub.isVisibility}"
            android:text="UserName: " />

        <TextView
            android:id="@+id/txtUserName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignBottom="@+id/textView"
            android:layout_marginLeft="72dp"
            android:layout_marginStart="72dp"
            android:layout_toEndOf="@+id/textView"
            android:layout_toRightOf="@+id/textView"
            android:text="@{modelGithub.owner.login}" />

        <!-- android:src="@mipmap/ic_launcher"
            app:error="@{@com.example.hientt17.example.R.drawable/ic_error}"
            app:imageUrl="@{modelGithub.owner.avatar_url}"-->
        <ImageView
            android:onClick="@{()->viewmodel.Onclick(view)}"
            android:id="@+id/avatar"
            android:layout_width="100dp"
            android:layout_height="100dp"
            android:layout_below="@+id/txtUserName"
            android:layout_centerHorizontal="true"
            android:layout_marginTop="58dp"
            />

        <android.support.v7.widget.RecyclerView
            android:id="@+id/listUser"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_alignParentLeft="true"
            android:layout_alignParentStart="true"
            android:layout_below="@+id/avatar"
            android:layout_marginTop="64dp" />


    </RelativeLayout>

</layout>
